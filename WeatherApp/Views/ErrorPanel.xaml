<UserControl x:Class="WeatherApp.Views.ErrorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        
    </StackPanel>
    <!--<StackPanel Visibility="{Binding Source={StaticResource Locator},Path=MainVm.HasErrorMessage,Converter={StaticResource BooleanCollapseConverter}}" Margin="5">
        <TextBox Text="{Binding Source={StaticResource Locator},Path=MainVm.ErrorMessage}" Style="{StaticResource ErrorMessageStyle}"/>
        <TextBox Text="Please check your internet connection." Style="{StaticResource ErrorMessageStyle}"/>
    </StackPanel>-->
</UserControl>
